name: CI_build

on: [push, pull_request, workflow_dispatch]

jobs:
  build_windows_msys2:

    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        #build_configuration: [Release, Debug]
        #build_platform: [x86_64, i686]
        build_configuration: [Release]
        
    steps:
    #- name: Checkout repo
      #uses: actions/checkout@v4

    - name: ECHO
      env:
        arek: "test2"
        arek2: ${{ github.event.pull_request.title }} -or ${{ github.event.head_commit.message }}
      run: |
        echo "Hellow World"
        echo ${{ matrix.build_configuration}}
        #echo ${{ matrix.build_platform}}
        echo ${{ github.event.head_commit.message }}
        echo ${{ github.event.pull_request.title }}
        $env:MESSAGE = ${{ github.event.pull_request.title }} -or ${{ github.event.head_commit.message }}
        Write-host $env:MESSAGE
        Write-Output $env:MESSAGE
        
        $MESSAGE2 = "testerek"
        Write-Output $MESSAGE2
        
        Write-Output dir env:
        $env:MESSAGE
        $arek = "test"
        echo ${{env.arek}}
        echo ${{env.arek2}}

        
        echo ${{ toJson(github) }}
